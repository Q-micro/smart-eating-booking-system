import { Box, Heading, Text } from "@chakra-ui/react";

export default function UsersPage() {
  return (
    <Box>
      <Heading size="lg">Users</Heading>
      <Text opacity={0.7}>
        User list + roles + ban controls will appear here.
      </Text>
    </Box>
  );
}
